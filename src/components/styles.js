import Colors from "../styles/Colors"

export const logoImg = (width) => ({ height: width, width: width })

export const bannerContainer = () => ({ display: "flex", backgroundColor: Colors.white, paddingLeft: '5%', paddingRight: '5%', flexDirection: 'column' })

export const absStar = () => ({ color: Colors.primary, fontSize: '2.5vw' })